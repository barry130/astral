# ============================================
# 生产环境参数（docker-compose 通过 env_file 引用）
# 仅 prod 启动加载；测试/本地不引用本文件，保持默认配置。
#
# 设计依据（1核 / 2GiB 腾讯云学生机）：
#   - JVM 堆 384MB（-Xms=-Xmx 固定） + 非堆约 150MB ≈ 550MB，剩余留给 OS/PostgreSQL/Redis
#   - UseSerialGC：单核 CPU 下最友好的 GC（无并行线程开销），避免 G1GC 在 1 核上抖动
#   - 激活 prod profile（application-prod.yml）：虚拟线程 + 线程参数 + 日志降级
# ============================================

# 激活生产 profile（application-prod.yml）
SPRING_PROFILES_ACTIVE=prod

# JVM 启动参数（覆盖 Dockerfile 默认值）
# 必须是单行：env_file 的多行反斜杠续行一旦未被解析器支持，JVM 会收到残缺参数而启动失败
JAVA_OPTS=-Xms384m -Xmx384m -XX:MetaspaceSize=96m -XX:MaxMetaspaceSize=128m -XX:+UseSerialGC -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/app/logs -XX:+ExitOnOutOfMemoryError -Duser.timezone=Asia/Shanghai
